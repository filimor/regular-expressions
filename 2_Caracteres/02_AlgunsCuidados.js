const textoUmaLinha = '...'; // aspas simples ou duplas
const textoMultiLinha = `
    linha 1
    linha 2
`; // backtick (crase)

// cuidado com o tab!
console.log('	'.match(/\s/g));

// Ao rodar o script, se houver texto selecionado, apenas aquele trecho de
// código será executado.